name: sqllite_demo
description: A new Flutter project on Drift Sqlite database.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  drift: ^1.0.1
  dartz: ^0.10.0
  equatable: ^2.0.3
  moor_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  moor_generator:
  build_runner:

flutter:

  uses-material-design: true

